{
  "name": "portafolio",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev --port 8787 --persist-to /tmp/ivavala-wrangler-state",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
